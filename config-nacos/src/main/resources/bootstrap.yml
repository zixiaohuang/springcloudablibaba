spring:
  application:
    name: com.hw.order
  cloud:
    nacos:
      server-addr: 8.134.165.135:8847
      username: nacos
      password: nacos
      config:
        refresh-enabled: true
        enabled: true
#        namespace: public

# 配置文件的优先级： profile > 默认配置文件  > extension-configs > shared-configs